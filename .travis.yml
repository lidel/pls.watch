language: node_js
node_js:
    - "8"
sudo: false
addons:
    firefox: latest
    hosts:
        - yt.127.0.0.1.xip.io
cache:
    yarn: true
    directories:
        - node_modules
install:
    # devDependencies from package.json
    - yarn --frozen-lockfile
script:
    - yarn test
    - yarn lint
